// Generated by CoffeeScript 1.3.3
(function() {
  var init, key, requirePaths, requiredFiles, value,
    __hasProp = {}.hasOwnProperty;

  requiredFiles = ['Core/GameManager', 'Core/GameMode', 'Core/Player', 'Core/NetworkManager', 'Game/Hero', 'Game/Match', 'Game/Round', 'Game/CpuRound', 'Game/PlayerRound', 'GameModes/MainMode', 'GameModes/MatchMode', 'GameModes/ResultsMode', 'GameModes/MarketMode'];

  requirePaths = {
    paths: {
      jquery: "//cdnjs.cloudflare.com/ajax/libs/jquery/1.7.2/jquery.min",
      underscore: "//cdnjs.cloudflare.com/ajax/libs/underscore.js/1.3.3/underscore-min",
      backbone: "//cdnjs.cloudflare.com/ajax/libs/backbone.js/0.9.2/backbone-min"
      /*text: "/js/lib/text"
      handlebars: "/js/lib/handlebars"
      */

    }
  };

  if (jsPaths) {
    for (key in jsPaths) {
      if (!__hasProp.call(jsPaths, key)) continue;
      value = jsPaths[key];
      if (key.slice(0, 4) === "lib/") {
        key = key.slice(4);
      }
      requirePaths.paths[key] = value;
    }
  }

  require.config({
    paths: requirePaths.paths,
    shim: {
      jquery: {
        exports: "$"
      },
      underscore: {
        exports: "_"
      },
      backbone: {
        deps: ["underscore", "jquery"],
        exports: "Backbone"
      }
    }
  });

  init = function(element) {
    var gameManager, player;
    player = new Player(window.playerJson);
    return gameManager = new GameManager();
  };

  require(requiredFiles, init);

}).call(this);
