// Generated by CoffeeScript 1.3.3

/*
User: mmarinucci
Copyright © mename 2014 All rights reserved
Player
*/


(function() {
  var Player, root;

  Player = (function() {

    Player.instance = null;

    function Player(playerJson) {
      Player.instance = this;
      this.username = null;
      this.id = null;
      this.heroes = null;
      this.activeSet = null;
      this.level = 0;
      this.xp = 0;
      this.deserializeJson(playerJson);
    }

    Player.prototype.deserializeJson = function(playerJson) {
      this.username = playerJson.username;
      this.id = playerJson.id;
      this.heroes = playerJson.heroes;
      this.activeSet = playerJson.activeSet;
      this.level = playerJson.level;
      return this.xp = playerJson.xp;
    };

    Player.prototype.getHero = function(heroId) {
      return this.heroes[heroId];
    };

    Player.getHeroByName = function(name) {
      var hero, res, _i, _len, _ref;
      res = null;
      _ref = window['heroesCatalog'];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        hero = _ref[_i];
        if (hero.name === name) {
          console.log('hero ' + name + ' found');
          res = hero;
          break;
        }
      }
      return res;
    };

    Player.prototype.addXp = function(xpDelta) {
      return console.log('add xp');
    };

    return Player;

  })();

  root = typeof exports !== "undefined" && exports !== null ? exports : window;

  root.Player = Player;

}).call(this);
